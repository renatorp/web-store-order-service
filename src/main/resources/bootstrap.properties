#The properties to configure the Config Client must necessarily be read in before the rest 
#of the application's configuration is read from the Config Server, during the bootstrap phase.

spring.application.name=web-store-order-service
server.port=8000

spring.cloud.config.uri = http://localhost:8888

eureka.client.service-url.default-zone=http://localhost:8761/eureka